const text ="Am the best ever the most violent the most erronous and most complete athlete of all time";
const pattern = /[a-zA-Z]+/g; //matching words

const matches = text.match(pattern);
console.log(matches)
